import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
        width: '50%',
        height: 48,
        backgroundColor: '#0004eeff',
        borderRadius: 4,
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'row',
    },
    title:{
        color: '#ffffff',
        fontSize: 14,
        fontWeight: '600',
    }
})